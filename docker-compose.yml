services:
  app:
    build: .
    image: shortlink:dev
    ports:
      - "8000:8000"
    environment:
      - BASE_URL=http://localhost:8000
    volumes:
      - shortlink-data:/app/data
    restart: unless-stopped

volumes:
  shortlink-data:
